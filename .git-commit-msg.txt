chore: migrate to standard Vite SPA + Express backend

- Remove Lovable-proprietary build config and SSR entrypoints
- Replace previewAuthStorage with standard Supabase localStorage auth
- Add standard Vite entry (index.html + src/main.tsx)
- Add Express backend (backend/) for demo account creation on Render
- Add vercel.json (SPA rewrites) and render.yaml (service definition)
- Add .env.example with all required variable names documented
- Update README with full deployment guide (Vercel + Render + Supabase)
- Build passes: tsc + vite build produce clean dist/
